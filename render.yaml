services:
  - type: web
    name: jobs-finder-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 80000"
    autoDeploy: true

